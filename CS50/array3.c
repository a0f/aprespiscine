#include <cs50.h>
#include <stdio.h>

void createarray(int array);

const int size = 5;

int main(void)
{
    //initialize array
    int array1[3] = {1, 2, 3};
    //add to array based on size
    reatearray(int array1[0]);
    printf("%i\n", array1[0]);
}

void createarray(int array)
{
    int array2;
    for (int i = 0; i < size; i++)
    {
       array2 = i;
    }
}
